#!# perl: deploy/code/m.test
use strict;
use warnings;

return sub
{
    my ( %param, @batch ) = @_;

    die "no path" unless -d "/tmp/path";
      

    print "=" x 30,"\n";
    return map{ $_ => 1 }@{$param{batch}};
};
